CLI command line interface

version
1.2 current version
1.1
1.0

maven:
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>1.2</version>
        </dependency>


Some important class or interface.
CommandLine : 

Parser : abstract class
	subclasses of Parser:
		Basic Parser
		Gnu Parse
		Posix Parser
		

CommandLineParser :
	A class that implements the CommandLineParser interface can parse a String array according to the Options specified and return a CommandLine.


* Option
	option Name: the name of the option.
	Long option name: the long name of the option.
	Has arguments: this indicates whether the option has a argument.
	argument number: the number of argument that this option can take.
	Description : description of the options, it is helpful to build a help information.
	Type : the type of the option.
	Is Required: to indicate whether this option is required.
	Value separator: the value separator of the options.
	Value or values: if the argument number is greater than 1, values will return.
* OptionBuilder: a builder to build an option.
	
* Options: a collection of Option.
* PatternOptionBuilder: a builder to build options with a single string.
Allows Options to be created from a single String. The pattern contains various single character flags and via an optional punctuation character, their expected type.

a	-a flag
b@	-b [classname]
c>	-c [filename]
d+	-d [classname] (creates object via empty contructor)
e%	-e [number] (creates Double/Long instance depeding on existing of a '.')
f/	-f [url]
g:	-g [string]
For example, the following allows command line flags of '-v -p string-value -f /dir/file'. The exclamation mark precede a mandatory option.

Options options = PatternOptionBuilder.parsePattern("vp:!f/");
TODO These need to break out to OptionType and also to be pluggable.



Help Formatter:


Type Handle :
	a tool class for type.
	Create Class:
	Create Date:
	Create File
	Create Files
	Create Number
	Create Object
	Create URL
	Create Value	

Exceptions
	Parser Exception
		Already Selected Exception
		Missing Argument Exception
		Missing Option Exception
		Unrecognized Option Exception


	
	
 
	

